/- content_for(:head)
/  == dato_meta_tags(page)
section
  .wrap
  br
    - dato.products.each do |product|
      .product_card
        h1 =product.title
        p.price = "Price from Dato $#{product.price}"
        p = lorem.sentence
        p
        =button_to "View Product", "https://upbeat-edison-9a7f8b.netlify.app/products/#{product.slug}"

        
